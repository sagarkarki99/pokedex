import 'package:flutter/material.dart';

class AllPokemonTab extends StatelessWidget {
  const AllPokemonTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      'All Pokemons',
    );
  }
}
